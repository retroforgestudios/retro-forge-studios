---
interface Props {
  name: string;
  class?: string;
}
const { name, class: className = "" } = Astro.props;

const LOGOS: Record<string, string> = {
  HubSpot: "/images/brands/hubspot.png",
  Claude: "/images/brands/claude.webp",
  YouTube: "/images/brands/youtube.png",
  WordPress: "/images/brands/wordpress.png",
  Gemini: "/images/brands/gemini.png",
  TikTok: "/images/brands/tiktok.png",
};

const src = LOGOS[name];
---

{src && <img src={src} alt={name} class:list={["w-auto", className || "h-7"]} width="800" height="200" loading="lazy" />}
